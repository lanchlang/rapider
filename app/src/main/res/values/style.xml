<resources>
    <style name="AppThemeNotActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>

        <item name="colorPrimary">@color/dark</item>
        <item name="colorPrimaryDark">@color/evenDarker</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:navigationBarColor">@color/evenDarker</item>
        <item name="android:windowBackground">@color/dark</item>
    </style>
    <style name="customPreference" parent="Preference.Material">
        <item name="android:layout">@layout/pref_custom_item</item>
    </style>
    <style name="customPreferenceTheme" parent="@style/PreferenceThemeOverlay">
        <!-- 自定义PreferenceFragment的Layout-->
        <item name="android:layout">@layout/fragment_preference</item>
        <item name="preferenceStyle">@style/customPreference</item>
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="preferenceTheme">@style/customPreferenceTheme</item>
        <item name="colorPrimary">@color/photonWhite</item>
        <item name="colorPrimaryDark">@color/photonWhite</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/evenDarker</item>
        <item name="android:windowBackground">@color/photonWhite</item>
    </style>

</resources>
